form(id='search' autocomplete="off")
  input(type='search' placeholder='Find a task...' name='name' class='searchbar' onkeypress="return event.keyCode != 13;")
  //- button(type='submit' class='searchbarBtn') Search
  div
    ul
      if (tasks)
        each task in tasks
          div
            li(class=`task hidden`)= task.content
